import { Flex } from "@chakra-ui/react";
import { ChevronLeftIcon } from "@chakra-ui/icons";

const Navigator = () => {
  return (
    <Flex>
      <ChevronLeftIcon />
    </Flex>
  );
};

export default Navigator;
